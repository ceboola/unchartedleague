<p class="fields">
  <%= f.select(:user_id, User.order("LOWER(psn_name) asc").collect { |x| [x, x.id] }) %>
  <%= f.hidden_field :_destroy %>
  <%= link_to_function "remove", "remove_fields(this)" %>
</p>
