<p class="fields">
  <%= f.select(:map_id, Map.order("LOWER(name) asc").collect { |x| [x, x.id] }) %>
  <%= f.hidden_field :_destroy %>
  <%= link_to_function "remove", "remove_fields(this)" %>
</p>
